server:
  port: 9002

spring:
  application:
    name: account

feign:
  httpclient:
    enabled: true
  compression:
    request:
      enabled: true
    response:
      enabled: true
  hystrix:
    enabled: true
